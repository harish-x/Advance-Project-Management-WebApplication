import { useGetAllTasksQuery } from '@/lib/features/task'
import React from 'react'

type Props = {}

const HomePage = (props: Props) => {
    // const [] = useGetAllTasksQuery({ projectId: id });
  return (
    <div>HomePage</div>
  )
}

export default HomePage